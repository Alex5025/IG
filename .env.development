# 開發用環境變數範本（不要把真實祕密提交到 git）
# 將此檔複製為 .env.local 並在本機載入（或直接 export 到 shell）

# webhook 驗證 token（與 Facebook App Dashboard 中設定的 verify token 相同）
IG_VERIFY_TOKEN=YeaMikoIsMyWifu

# App Secret 用於 X-Hub-Signature-256 驗證（可留空於開發時，但生產必填）
IG_APP_SECRET=fcaba18be0d3bfe851dafadc50e86892

# Page / Messaging access token（用於發送訊息）
IG_PAGE_ACCESS_TOKEN=your_dev_page_access_token

# short token（若你使用短期 token）
SHORT_TOKEN=your_dev_short_token

# 本機測試用 base url
BASE_URL=https://9976ee9973a7.ngrok-free.app

# 若要使用 send-ig-message.sh，請提供接收者 id
IG_RECIPIENT_ID=1098847598895224
